/**
 * @deprecated Import from `@/lib/validation` instead.
 * Kept so existing `@/lib/validations` imports keep working.
 */
export {
  loginSchema,
  signupSchema,
  otpSchema,
  profileSchema,
  forgotPasswordSchema,
  resetPasswordSchema,
} from './validation/schemas/auth';
