# Environment variables for the application
# Copy this file to .env.local and update with your actual values

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Dev only: bypass login API until backend is integrated
# - Local dev: bypass is on by default
# - Vercel: bypass is on by default (VERCEL=1) until you set this to false
# NEXT_PUBLIC_DEV_BYPASS_AUTH=false

# Auth Configuration
NEXT_PUBLIC_AUTH_DOMAIN=your-auth-domain.com

# AWS S3 Configuration (for media)
NEXT_PUBLIC_AWS_S3_BUCKET=projectk-media
NEXT_PUBLIC_AWS_REGION=ap-south-1

# Payment Configuration
NEXT_PUBLIC_RAZORPAY_KEY_ID=your-razorpay-key

# Other public variables
NEXT_PUBLIC_APP_NAME=Project K
